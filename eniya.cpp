/**
 *    author: mohin    
**/

#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n{}, a{}, b{}, ans{};
    cin >> n >> a >> b;
    ans = 2 * n * a * b;
    cout << ans << endl;
    return 0;
}