/**
 *    author: mohin    
**/

#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int a{}, b{}, sum{};
    cin >> a >> b;
    sum = a + b;
    cout << sum << endl;
    return 0;
}